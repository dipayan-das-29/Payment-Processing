spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    driver-class-name: org.postgresql.Driver
  kafka:
    consumer:
      auto-offset-reset: earliest
  data:
    redis:
      timeout: 2s
